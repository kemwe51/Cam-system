{
  "name": "@cam/model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node --eval \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.build.json",
    "test": "vitest run src/index.test.ts"
  },
  "dependencies": {
    "@cam/geometry2d": "file:../geometry2d",
    "@cam/shared": "file:../shared",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cam/engine": "file:../engine"
  }
}
